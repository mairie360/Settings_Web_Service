name: Settings Web Service CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/frontend-cicd.yml@v3.0.0
    with:
      package_name: "settings-front"
      node_version: "23"
      cicd_version: "v3.0.0"
    secrets: inherit
