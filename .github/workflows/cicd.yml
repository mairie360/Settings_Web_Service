name: Settings Web Service CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/frontend-cicd.yml@v1.13.2
    with:
      package_name: "settings-front"
      node_version: "23"
      cicd_version: "v1.13.2"
    secrets: inherit
